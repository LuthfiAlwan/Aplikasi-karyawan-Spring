// Contoh JavaScript dengan jQuery untuk berinteraksi dengan backend

// Fungsi untuk menampilkan data karyawan
function displayEmployees() {
    // Lakukan permintaan ke backend untuk mendapatkan data karyawan
    // Tampilkan data dalam tabel di halaman HTML
}

// Fungsi untuk menambah karyawan
function addEmployee(employeeData) {
    // Kirim permintaan POST ke backend untuk menambah karyawan
    // Perbarui tampilan setelah menambah data
}

// Fungsi untuk mengedit karyawan
function editEmployee(employeeId, updatedEmployeeData) {
    // Kirim permintaan PUT ke backend untuk mengubah data karyawan
    // Perbarui tampilan setelah mengedit data
}

// Fungsi untuk menghapus karyawan
function deleteEmployee(employeeId) {
    // Kirim permintaan DELETE ke backend untuk menghapus karyawan
    // Perbarui tampilan setelah menghapus data
}
